    /**
     * the property to test
     */
    private _relPropName: EnhancedSet<PropType> | undefined;

    /**
     * Gets the value of the relPropName property.
     * @returns The value of the relPropName property
     */
    get relPropName(): EnhancedSet<PropType> | undefined {
        return this.getSetProperty('_relPropName');
    }

    /**
     * Add one item to the collection property relPropName
     * @param newValue the new value to add into the property
     */
    addOneUPropName(newValue: PropType): void {
        return this.addOneSetProperty('_relPropName', newValue);
    }

    /**
     * Remove one item from the collection property relPropName
     * @param oldValue the value to remove from the property
     */
    removeOneUPropName(oldValue: PropType): void {
        return this.removeOneSetProperty('_relPropName', oldValue);
    }

    /**
     * Add a set of relPropName into to the collection property relPropName
     * @param newValues the new values to add into the property
     */
    addManyUPropName(newValues: EnhancedSet<PropType>): void {
        return this.addManySetProperty('_relPropName', newValues);
    }

    /**
     * Remove a set of relPropName from the collection property relPropName
     * @param oldValues the values to remove from the property
     */
    removeManyUPropName(oldValues: EnhancedSet<PropType>): void {
        return this.removeManySetProperty('_relPropName', oldValues);
    }

    /**
     * Clear all relPropName from the collection property relPropName
     */
    clearUPropName(): void {
        return this.clearSetProperty('_relPropName');
    }

    /**
     * get the numbre of entries inside of relPropName
     * @returns the current numbre of entries in relPropName
     */
    sizeofUPropName(): number {
        return (this._relPropName == null) ? 0 : this._relPropName.size;
    }

    /**
     * does relPropName contain the given entry already?
     * @param entryToCheck the object to look for in the set
     * @returns true if entryToCheck is currently inside of relPropName
     */
    relPropNameHas(entryToCheck: PropType): boolean {
        return (this._relPropName == null) ? false : this._relPropName.has(entryToCheck);
    }